namespace DeveloperTask.Task1
{
    internal class Car : Vehicle
    {
        public Car() : base()
        {
        }

        public Car(float topSpeed, string name, int passengerCount) : base(topSpeed, name, passengerCount)
        {
        }
    }
}
